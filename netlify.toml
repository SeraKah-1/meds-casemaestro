
---

**`netlify.toml`**
```toml
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# API proxy (we'll add functions in Part 5)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
